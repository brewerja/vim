My vim configuration.

To activate the vimrc and gvimrc files:
    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

To update all submodules:
    git submodule foreach git pull origin master
